import React from 'react';
import PropTypes from 'prop-types';

const AudienceDelete = () => {
  return (
    <React.Fragment>
      AudienceDelete
    </React.Fragment>
  );
};

AudienceDelete.propTypes = {};

export default AudienceDelete;
