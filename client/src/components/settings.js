import React from 'react';
import PropTypes from 'prop-types'

const Settings = () => {
  return (
    <React.Fragment>
      Settings
    </React.Fragment>
  );
};

Settings.propTypes = {};

export default Settings;
