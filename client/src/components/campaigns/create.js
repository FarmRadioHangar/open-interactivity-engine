import React from 'react';
import PropTypes from 'prop-types'

const CampaignsCreate = () => {
  return (
    <React.Fragment>
      CampaignsCreate
    </React.Fragment>
  );
};

CampaignsCreate.propTypes = {};

export default CampaignsCreate;
