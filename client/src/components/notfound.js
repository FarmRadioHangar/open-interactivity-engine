import React from 'react';
import PropTypes from 'prop-types';

const NotFound = () => {
  return (
    <React.Fragment>
      Page not found
    </React.Fragment>
  );
};

NotFound.propTypes = {};

export default NotFound;
